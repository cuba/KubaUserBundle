{% extends 'KubaUserBundle::layout.html.twig' %}

{% block content %}
    <h1>Registration</h1>
    <form action="{{ path('kuba_user_registration') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}

        <input type="submit" />
    </form>
{% endblock %}
